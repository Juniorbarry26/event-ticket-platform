spring:
  application:
    name: ${SPRING_APPLICATION_NAME:tickets}
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    com.alsaineybarry.tickets: INFO
    org.springframework.security: WARN
    org.hibernate: WARN
